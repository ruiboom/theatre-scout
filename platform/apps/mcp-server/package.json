{
  "name": "mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "echo 'no lint configured'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo 'wrangler builds on deploy'",
    "clean": "rm -rf .wrangler dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0",
    "@platform/shared": "workspace:*",
    "agents": "^0.0.95",
    "hono": "^4.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.86.0"
  }
}
